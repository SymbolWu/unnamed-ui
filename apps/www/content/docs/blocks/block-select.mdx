---
title: BlockSelect
description: 选择器
author: AF
---

<ComponentPreview name="block-select-default" description="选择器" className="mb-4" />

选择器

## 快速开始

```tsx
import { BlockSelect } from "@/registry/wuhan/composed/block-select";

export function Example() {
  return <BlockSelect />;
}
```

## 安装

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash
npx shadcn@latest add "https://unnamed-ui.com/r/wuhan/block-select"
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>复制以下代码到你的项目</Step>

<ComponentSource name="block-select" />

<Step>更新导入路径以匹配你的项目设置</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 代码演示

### 基础用法

<ComponentPreview
  name="block-select-default"
  description="基础用法"
  className="mb-4"
/>

## API

### BlockSelect

选择器

#### Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `children` | `ReactNode` | `"BlockSelect"` | 组件内容 |
| `className` | `string` | - | 容器自定义类名 |

## 原语组件

BlockSelect 组件基于以下原语组件构建：

- `BlockSelectContainerPrimitive`: 容器组件

如需更灵活的定制，可以直接使用这些原语组件。
