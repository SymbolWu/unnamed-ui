# 批量安装组件指南

本文档介绍如何一次性安装所有 Wuhan UI 组件。

## ⚠️ 重要说明

### 关于域名配置

- **本地开发**：使用 `http://localhost:3000`
- **生产环境**：使用你的 Vercel 域名，例如 `https://your-project.vercel.app`
- **自定义域名**：使用你配置的自定义域名

### 自动覆盖安装

所有安装命令都添加了 `-y` 参数，会**自动覆盖**现有组件，无需手动输入确认。

---

## 方法一：使用 Node.js 脚本（最推荐）

✅ 适用于任何用户，支持远程域名，自动覆盖安装

### 使用 Vercel 部署的组件库

```bash
# 从 GitHub 下载并执行（一行命令）
curl -fsSL https://raw.githubusercontent.com/your-username/unnamed-ui/main/apps/www/scripts/install/install-components.cjs | node - https://your-project.vercel.app

# 或者下载到本地后执行
curl -O https://raw.githubusercontent.com/your-username/unnamed-ui/main/apps/www/scripts/install/install-components.cjs
node install-components.cjs https://your-project.vercel.app
```

### 本地开发

```bash
# 在项目根目录
cd apps/www/scripts/install
node install-components.cjs http://localhost:3000

# 或使用默认的 localhost:3000
node install-components.cjs
```

---

## 方法二：使用 Bash 脚本

✅ 适用于 macOS/Linux 用户

### 使用 Vercel 部署的组件库

```bash
# 从 GitHub 下载并执行
curl -fsSL https://raw.githubusercontent.com/your-username/unnamed-ui/main/apps/www/scripts/install/install-all-components.sh | bash -s -- https://your-project.vercel.app

# 或下载到本地后执行
curl -O https://raw.githubusercontent.com/your-username/unnamed-ui/main/apps/www/scripts/install/install-all-components.sh
chmod +x install-all-components.sh
./install-all-components.sh https://your-project.vercel.app
```

### 本地开发

```bash
# 在项目根目录
cd apps/www/scripts/install
./install-all-components.sh
```

该脚本会按照分类依次安装所有 54 个组件：
- 6 个基础组件（Avatar、Progress、Tag 等）
- 6 个按钮组件（BlockButton、IconButton 等）
- 7 个气泡组件（Message、MessageList 等）
- 7 个卡片组件（DocumentCard、FileCard 等）
- 7 个输入组件（BlockInput、Sender 等）
- 5 个布局组件（Sidebar、PageHeader 等）
- 3 个列表组件（HistoryItem 等）
- 6 个步骤组件（ThinkingProcess 等）

---

## 方法三：为其他用户提供安装命令

如果你已经部署到 Vercel，可以提供以下一键命令给其他用户：

### 方式 A：使用 Node.js 脚本（推荐）

```bash
# 1. 下载安装脚本
curl -O https://raw.githubusercontent.com/your-username/unnamed-ui/main/apps/www/scripts/install/install-components.cjs

# 2. 执行安装（替换为你的域名）
node install-components.cjs https://your-project.vercel.app
```

### 方式 B：使用单行命令（最快）

```bash
# 直接下载并执行（需要替换域名）
curl -fsSL https://raw.githubusercontent.com/your-username/unnamed-ui/main/apps/www/scripts/install/install-components.cjs | node - https://your-project.vercel.app
```

### 方式 C：提供 Bash 脚本

```bash
# 下载并执行 Bash 脚本
curl -fsSL https://raw.githubusercontent.com/your-username/unnamed-ui/main/apps/www/scripts/install/install-all-components.sh | bash -s -- https://your-project.vercel.app
```

---

## 方法四：选择性安装

## 方法四：选择性安装

如果你只需要安装特定分类的组件，可以：

### 1. 编辑脚本文件

编辑 `install-all-components.sh` 或 `install-components.cjs`，注释掉不需要的部分。

### 2. 手动安装单个组件

```bash
# 从 Vercel 部署的组件库安装
npx shadcn@latest add -y "https://your-project.vercel.app/r/wuhan/avatar.json"

# 从本地开发环境安装
npx shadcn@latest add -y "http://localhost:3000/r/wuhan/avatar.json"
```

### 3. 按分类安装示例

```bash
# 设置基础 URL
BASE_URL="https://your-project.vercel.app"

# 只安装基础组件
npx shadcn@latest add -y "$BASE_URL/r/wuhan/avatar.json"
npx shadcn@latest add -y "$BASE_URL/r/wuhan/progress.json"
npx shadcn@latest add -y "$BASE_URL/r/wuhan/status-tag.json"
npx shadcn@latest add -y "$BASE_URL/r/wuhan/tag.json"
npx shadcn@latest add -y "$BASE_URL/r/wuhan/tooltip.json"
npx shadcn@latest add -y "$BASE_URL/r/wuhan/custom-sources.json"
```

---

## 部署后的配置步骤

### 1. 更新 README.md

在你的项目 README 中添加安装说明：

\`\`\`markdown
## 安装组件

### 安装所有组件

\`\`\`bash
# 下载安装脚本
curl -O https://raw.githubusercontent.com/your-username/your-repo/main/install-components.cjs

# 执行安装
node install-components.cjs https://your-project.vercel.app
\`\`\`

### 安装单个组件

\`\`\`bash
npx shadcn@latest add -y "https://your-project.vercel.app/r/wuhan/[组件名].json"
\`\`\`
\`\`\`

### 2. 提供快速安装命令

创建一个简短的域名或使用 CDN，让用户可以更简单地安装：

```bash
# 示例：使用短域名
curl -fsSL https://ui.yourdomain.com/install.js | node - https://ui.yourdomain.com
```

---

## 注意事项

### ✅ 自动覆盖安装

- 所有命令都使用了 `-y` 参数
- 安装时会**自动覆盖**现有组件
- **无需**手动输入 `y` 确认

### 🌐 域名配置

- **本地开发**：`http://localhost:3000`（需要先运行 `pnpm dev`）
- **Vercel 部署**：`https://your-project.vercel.app`
- **自定义域名**：配置后使用你的域名

### ⏱️ 安装时间

- 完整安装所有 54 个组件大约需要 **3-5 分钟**
- 受网络速度影响

### 📁 安装路径

- 组件会安装到项目的 `components` 目录下
- 具体路径由 `components.json` 配置决定

---

---

## 组件列表

所有 54 个组件的完整列表：

### 基础组件 (base/) - 6 个
- avatar
- progress
- status-tag
- tag
- tooltip
- custom-sources

### 按钮组件 (buttons/) - 6 个
- block-button
- icon-button
- prompt
- quick-action
- suggestion
- toggle-button

### 气泡组件 (bubbles/) - 9 个
- avatar-header
- confirm-panel
- dynamic-form
- feedback
- message
- message-list
- quote-content
- task-list
- welcome

### 卡片组件 (cards/) - 7 个
- agent-card
- document-card
- file-card
- goal-card
- report-card
- select-card
- task-card

### 输入组件 (inputs/) - 7 个
- block-input
- block-select
- checkbox
- radio
- sender
- responsive-sender
- upload

### 布局组件 (layout/) - 5 个
- divider
- page-header
- sidebar
- sources-sidebar
- triple-split-pane

### 列表组件 (lists/) - 3 个
- attachment-list
- component-panel
- history-item

### 步骤组件 (steps/) - 6 个
- block-accordion
- deep-thinking
- execution-result
- thinking-process
- thinking-process-01
- thinking-step-item

---

## 故障排除

### ❌ 问题：连接失败

**本地开发环境**：
```bash
# 确保开发服务器正在运行
pnpm dev
```

**生产环境**：
- 检查 Vercel 域名是否正确
- 确认组件 JSON 文件可以访问
- 访问 `https://your-domain.vercel.app/r/wuhan/avatar.json` 测试

### ❌ 问题：组件安装失败

检查 `components.json` 配置文件是否存在：
```bash
# 如果不存在，先初始化
npx shadcn@latest init
```

### ❌ 问题：缺少依赖

某些组件可能需要额外的依赖，按提示安装：
```bash
npm install [缺少的依赖包]
```

### ❌ 问题：覆盖安装时仍然提示确认

确保使用了 `-y` 参数：
```bash
# ✅ 正确
npx shadcn@latest add -y "url"

# ❌ 错误（会提示确认）
npx shadcn@latest add "url"
```

### ❌ 问题：脚本执行权限不足

```bash
# 添加执行权限
chmod +x install-all-components.sh
chmod +x install-components.cjs
```

---

## 更新组件

要更新已安装的组件，只需重新运行对应的安装命令即可（会自动覆盖）：

```bash
# 更新单个组件
npx shadcn@latest add -y "https://your-domain.vercel.app/r/wuhan/avatar.json"

# 更新所有组件
node install-components.cjs https://your-domain.vercel.app
```

---

## 示例：完整工作流程

### 对于组件库维护者（你）

1. 开发组件
2. 部署到 Vercel
3. 获取 Vercel 域名（例如：`https://unnamed-ui.vercel.app`）
4. 在 README 中提供安装命令

### 对于组件库使用者（其他人）

```bash
# 1. 下载安装脚本
curl -O https://raw.githubusercontent.com/your-username/unnamed-ui/main/install-components.cjs

# 2. 执行安装（一次性安装所有组件）
node install-components.cjs https://unnamed-ui.vercel.app

# 3. 开始使用组件
# 组件已经安装到你的项目中了
```

或者更简单的一行命令：

```bash
curl -fsSL https://raw.githubusercontent.com/your-username/unnamed-ui/main/install-components.cjs | node - https://unnamed-ui.vercel.app
```

---

## 推荐的文档更新

建议在你的主 README.md 中添加安装说明：

```markdown
## 快速开始

### 安装所有组件

\`\`\`bash
curl -fsSL https://raw.githubusercontent.com/your-username/unnamed-ui/main/install-components.cjs | node - https://unnamed-ui.vercel.app
\`\`\`

### 安装单个组件

\`\`\`bash
npx shadcn@latest add -y "https://unnamed-ui.vercel.app/r/wuhan/avatar.json"
\`\`\`

查看 [完整安装指南](INSTALL_COMPONENTS.md) 了解更多选项。
```

---

## 相关文件

所有安装相关文件位于 `apps/www/scripts/install/` 目录：

- `install-components.cjs` - Node.js 安装脚本（推荐）
- `install-all-components.sh` - Bash 安装脚本
- `test-components-urls.sh` - URL 测试脚本
- `components-list.json` - 组件列表配置文件
- `README.md` - 脚本使用说明

文档文件位于 `apps/www/docs/` 目录：

- `INSTALL_COMPONENTS.md` - 本文档（完整安装指南）
- `QUICK_REFERENCE.md` - 快速参考命令
