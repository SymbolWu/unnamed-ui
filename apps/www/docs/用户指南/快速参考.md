# 快速参考 - 组件安装命令

## 🚀 一键安装（推荐）

### 使用 Node.js 脚本

```bash
# 从 GitHub 直接安装（替换为你的域名）
curl -fsSL https://raw.githubusercontent.com/your-username/unnamed-ui/main/apps/www/scripts/install/install-components.js | node - https://your-domain.vercel.app

# 或者下载后安装
curl -O https://raw.githubusercontent.com/your-username/unnamed-ui/main/apps/www/scripts/install/install-components.js
node install-components.js https://your-domain.vercel.app
```

### 使用 Bash 脚本

```bash
# 本地开发（默认 localhost:3000）
cd apps/www/scripts/install
./install-all-components.sh

# 使用自定义域名
./install-all-components.sh https://your-domain.vercel.app
```

---

## 📦 单个组件安装

```bash
# 格式（注意 -y 参数自动覆盖）
npx shadcn@latest add -y "https://your-domain.vercel.app/r/wuhan/[组件名].json"

# 示例
npx shadcn@latest add -y "https://your-domain.vercel.app/r/wuhan/avatar.json"
npx shadcn@latest add -y "https://your-domain.vercel.app/r/wuhan/button.json"
```

---

## 🔧 测试和验证

### 测试组件 JSON 可访问性

```bash
# 本地测试
cd apps/www/scripts/install
./test-components-urls.sh

# 测试生产环境
./test-components-urls.sh https://your-domain.vercel.app
```

### 验证安装

```bash
# 检查组件目录
ls -la components/ui/

# 测试导入组件
cat > test-import.tsx << 'EOF'
import { Avatar } from "@/components/ui/avatar";
import { Button } from "@/components/ui/button";
EOF
```

---

## 📝 关键参数说明

| 参数 | 说明 |
|------|------|
| `-y` | 自动确认，覆盖安装时不提示 |
| `BASE_URL` | 组件库域名（本地: `http://localhost:3000`，生产: 你的 Vercel 域名） |

---

## 🌐 域名配置

| 环境 | 域名示例 |
|------|----------|
| 本地开发 | `http://localhost:3000` |
| Vercel 默认 | `https://your-project.vercel.app` |
| 自定义域名 | `https://ui.yourdomain.com` |

---

## ⚡ 常用命令速查

```bash
# 本地开发 - 启动服务器
pnpm dev

# 本地开发 - 安装所有组件
cd apps/www/scripts/install
./install-all-components.sh

# 生产环境 - 安装所有组件（Node.js）
curl -fsSL https://raw.githubusercontent.com/your-username/unnamed-ui/main/apps/www/scripts/install/install-components.js | node - https://your-domain.vercel.app

# 生产环境 - 安装所有组件（Bash）
curl -fsSL https://raw.githubusercontent.com/your-username/unnamed-ui/main/apps/www/scripts/install/install-all-components.sh | bash -s -- https://your-domain.vercel.app

# 安装单个组件
npx shadcn@latest add -y "https://your-domain.vercel.app/r/wuhan/avatar.json"

# 测试组件 URLs
cd apps/www/scripts/install
./test-components-urls.sh https://your-domain.vercel.app

# 更新组件（重新安装）
curl -fsSL https://raw.githubusercontent.com/your-username/unnamed-ui/main/apps/www/scripts/install/install-components.js | node - https://your-domain.vercel.app
```

---

## 📚 文档资源

- [完整安装指南](INSTALL_COMPONENTS.md) - 详细的安装文档
- [组件列表](../scripts/install/components-list.json) - 所有可用组件的 JSON 配置
- [安装脚本说明](../scripts/install/README.md) - 脚本使用说明

---

## 🎯 给其他用户的快速开始

如果你是组件库使用者，只需执行：

```bash
# 一行命令完成安装（替换域名）
curl -fsSL https://raw.githubusercontent.com/your-username/unnamed-ui/main/apps/www/scripts/install/install-components.js | node - https://your-domain.vercel.app
```

---

## 💡 提示

1. ✅ 所有命令都使用 `-y` 参数，自动覆盖无需确认
2. 🌐 部署到 Vercel 后，替换所有 `localhost:3000` 为你的域名
3. 📦 54 个组件，完整安装约需 3-5 分钟
4. 🔄 重新运行安装命令即可更新组件
5. 📁 安装脚本位于 `apps/www/scripts/install/` 目录
