# 📁 文件组织结构说明

## 目录结构

```
unnamed-ui/
├── apps/www/
│   ├── scripts/
│   │   └── install/              ← 安装脚本目录
│   │       ├── README.md         - 脚本使用说明
│   │       ├── install-components.cjs      ⭐ Node.js 安装脚本（推荐）
│   │       ├── install-all-components.sh  ✅ Bash 安装脚本
│   │       ├── test-components-urls.sh    🔧 测试脚本
│   │       └── components-list.json       📋 组件列表配置
│   │
│   └── docs/                     ← 文档目录
│       ├── INSTALL_COMPONENTS.md - 完整安装指南
│       ├── QUICK_REFERENCE.md    - 快速参考命令
│       └── ... (其他开发文档)
│
└── README_INSTALLATION_SNIPPET.md ⚠️ 临时文件（添加到 README 后可删除）
```

## 文件分类

### ✅ 核心文件（保留）

#### 安装脚本（`apps/www/scripts/install/`）

1. **install-components.cjs** ⭐ 推荐
   - Node.js 脚本，跨平台
   - 带进度显示和错误处理
   - 用法：`node install-components.cjs <BASE_URL>`

2. **install-all-components.sh** ✅ 保留
   - Bash 脚本，macOS/Linux
   - 对某些用户更方便
   - 用法：`./install-all-components.sh <BASE_URL>`

3. **test-components-urls.sh** 🔧 工具
   - 测试组件 JSON 可访问性
   - 部署前验证使用
   - 用法：`./test-components-urls.sh <BASE_URL>`

4. **components-list.json** 📋 配置
   - 组件列表和分类
   - JSON 格式配置文件

5. **README.md** 📖 说明
   - 安装脚本目录的说明文档

#### 文档文件（`apps/www/docs/`）

6. **INSTALL_COMPONENTS.md** 📚 用户文档
   - 完整的安装指南
   - 面向组件库使用者

7. **QUICK_REFERENCE.md** 📚 用户文档
   - 快速参考命令
   - 常用命令速查

### ⚠️ 临时文件

8. **README_INSTALLATION_SNIPPET.md**（项目根目录）
   - 用于添加到主 README.md 的内容片段
   - 添加后可以删除
   - 位于项目根目录

## 文件用途说明

### install-components.cjs vs install-all-components.sh

**问题**：是否 `install-all-components.sh` 没用了？

**答**：**两个都有用**，针对不同场景：

| 特性 | install-components.cjs | install-all-components.sh |
|------|----------------------|---------------------------|
| 平台支持 | ✅ Windows/macOS/Linux | ⚠️ 仅 macOS/Linux |
| 进度显示 | ✅ 有 | ❌ 无 |
| 错误统计 | ✅ 有 | ❌ 无 |
| 使用便捷性 | 需要 Node.js | Shell 直接运行 |
| 推荐度 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |

**建议**：
- 对外推荐使用 `install-components.cjs`（更好的用户体验）
- 保留 `install-all-components.sh` 给习惯 Shell 的用户

## 使用场景

### 场景 1：本地开发（你自己）

```bash
# 启动开发服务器
pnpm dev

# 在另一个终端
cd apps/www/scripts/install
node install-components.cjs
# 或
./install-all-components.sh
```

### 场景 2：Vercel 部署后（其他用户）

```bash
# 推荐给用户的命令
curl -fsSL https://raw.githubusercontent.com/your-username/unnamed-ui/main/apps/www/scripts/install/install-components.cjs | node - https://your-domain.vercel.app
```

### 场景 3：测试验证

```bash
cd apps/www/scripts/install
./test-components-urls.sh https://your-domain.vercel.app
```

## 下一步操作

### 1. 添加安装说明到主 README

将 `README_INSTALLATION_SNIPPET.md` 的内容添加到项目根目录的 `README.md` 中。

### 2. 删除临时文件

添加到主 README 后：

```bash
rm README_INSTALLATION_SNIPPET.md
```

### 3. 提交更改

```bash
git add apps/www/scripts/install/
git add apps/www/docs/INSTALL_COMPONENTS.md
git add apps/www/docs/QUICK_REFERENCE.md
git commit -m "refactor: reorganize installation scripts and documentation"
```

### 4. 更新主 README（如果还没有）

在主 `README.md` 中添加安装说明章节。

## 维护说明

### 添加新组件

当添加新组件时，需要更新：

1. ✅ `components-list.json` - 添加组件名到对应分类
2. ✅ `install-components.cjs` - 在 components 对象中添加
3. ✅ `install-all-components.sh` - 添加安装命令
4. ✅ `test-components-urls.sh` - 添加到测试列表

### 更新文档

如果安装方式有变化，需要更新：

1. ✅ `apps/www/docs/INSTALL_COMPONENTS.md`
2. ✅ `apps/www/docs/QUICK_REFERENCE.md`
3. ✅ `apps/www/scripts/install/README.md`

## 总结

| 文件 | 状态 | 位置 | 用途 |
|------|------|------|------|
| install-components.cjs | ⭐ 核心 | scripts/install/ | Node.js 安装脚本 |
| install-all-components.sh | ✅ 保留 | scripts/install/ | Bash 安装脚本 |
| test-components-urls.sh | 🔧 工具 | scripts/install/ | 测试脚本 |
| components-list.json | 📋 配置 | scripts/install/ | 组件列表 |
| install/README.md | 📖 文档 | scripts/install/ | 脚本说明 |
| INSTALL_COMPONENTS.md | 📚 文档 | docs/ | 完整安装指南 |
| QUICK_REFERENCE.md | 📚 文档 | docs/ | 快速参考 |
| README_INSTALLATION_SNIPPET.md | ⚠️ 临时 | 项目根目录 | 待添加到 README |

**所有文件都已按照最佳实践重新组织！** ✨
